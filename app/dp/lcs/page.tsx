import DPVisualizer from "@/components/DPVisualizer";

export const metadata = { title: "Longest Common Subsequence – CodeCookbook" };

export default function LCSPage() {
  return <DPVisualizer algorithm="lcs" />;
}
