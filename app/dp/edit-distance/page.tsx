import DPVisualizer from "@/components/DPVisualizer";

export const metadata = { title: "Edit Distance (Levenshtein) – CodeCookbook" };

export default function EditDistancePage() {
  return <DPVisualizer algorithm="edit-distance" />;
}
