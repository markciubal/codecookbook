import SortingVisualizer from "@/components/SortingVisualizer";

export const metadata = { title: "Heap Sort – CodeCookbook" };

export default function HeapSortPage() {
  return <SortingVisualizer algorithm="heap" />;
}
