import SortingVisualizer from "@/components/SortingVisualizer";

export const metadata = { title: "Cycle Sort – CodeCookbook" };

export default function CycleSortPage() {
  return <SortingVisualizer algorithm="cycle" />;
}
