import SortingVisualizer from "@/components/SortingVisualizer";

export const metadata = { title: "Tim Sort – CodeCookbook" };

export default function TimSortPage() {
  return <SortingVisualizer algorithm="timsort" />;
}
