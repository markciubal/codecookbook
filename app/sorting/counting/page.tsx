import SortingVisualizer from "@/components/SortingVisualizer";

export const metadata = { title: "Counting Sort – CodeCookbook" };

export default function CountingSortPage() {
  return <SortingVisualizer algorithm="counting" />;
}
