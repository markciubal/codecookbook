import GeometryVisualizer from "@/components/GeometryVisualizer";

export const metadata = { title: "Convex Hull (Jarvis March) – CodeCookbook" };

export default function JarvisMarchPage() {
  return <GeometryVisualizer algorithm="jarvis-march" />;
}
